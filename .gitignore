# JavaScript
node_modules/

# Hook scan logs (written by scan-secrets.sh at session end)
logs/

# Vale — downloaded style packages (not committed; fetched by `vale sync`)
.github/vale/styles/Vale/
.github/vale/styles/Google/
.github/vale/styles/write-good/

# OS / Editor artefacts
.DS_Store
Thumbs.db
*.bak
*.orig
*~

# Python bytecode cache
__pycache__/
*.pyc

# Secrets and credentials — prevent accidental commits
.env
.env.*
*.pem
*.key
*.p12
*.pfx
secrets.yml
secrets.yaml
.aws/credentials

# Heartbeat orchestrator sidecar state
.copilot/workspace/.heartbeat-session
.copilot/workspace/.heartbeat-events.jsonl
.copilot/workspace/state.json
